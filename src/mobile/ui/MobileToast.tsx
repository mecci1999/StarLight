import { mobileFeedback } from '#/services/mobileFeedback'

export const MobileToast = mobileFeedback

export const { success, error, warning, info, loading, close, confirm } = mobileFeedback

export default MobileToast
